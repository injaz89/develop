{
  "name": "hiring-fullstack-todo",
  "version": "1.0.0",
  "description": "Full-stack TODO application monorepo",
  "private": true,
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "install-all": "npm install && npm install --prefix client && npm install --prefix server",
    "start-dev": "concurrently \"npm run dev --prefix server\" \"npm start --prefix client\"",
    "start-server": "npm run dev --prefix server",
    "start-client": "npm start --prefix client"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
